<div class="card">
    <div class="card-body card-padding">
        <% flash.each do |type, msg| %>
        <div>
            <div class="alert alert-success" role="alert">
                <button type="button" class="close" data-dismiss="alert">&times;</button>
                <%= msg %>
            </div>

        </div>
        <% end %>
        Aqui ira la tabla de guías
    </div>
</div>